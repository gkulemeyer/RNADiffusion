# @package _global_

pipeline:
  run_training: true
  run_testing: false
  run_ensemble_generation: false
  run_ensemble_analysis: false

training:
  batch_size: 4
  lr: 0.001
  epochs: 3
  num_workers: 2
  optimizer:
    type: adam
    weight_decay: 0.01
